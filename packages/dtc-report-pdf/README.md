# DTC Report PDF

Export a full diagnostic trouble-code scan as a clean, shareable PDF report
with vehicle info and timestamps. Opens your browser's print dialog — choose
"Save as PDF".

Capabilities: `read-vin`, `read-dtc`. No network access, no writes to the ECU.
