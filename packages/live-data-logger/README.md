# Live Data Logger

Record live sensor streams from any ECU to CSV with timestamps. Pick your
PIDs, hit Start, hit Stop & save. Files land in the app's documents folder.

Capabilities: `live-data`, `filesystem`. No network access, no ECU writes.
